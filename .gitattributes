# Normalize all text files to LF in working tree (consistent across OS)
* text=auto eol=lf

# Windows-specific scripts (keep CRLF if needed)
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
